# Development Environment Variables Template
#
# This file serves as a template for setting up your local development environment.
# It includes all variables needed for the Meen Ma3ana charity platform.
#
# To get started:
# 1. Copy this file to `.env.local`
# 2. Fill in your actual values from the Supabase dashboard
# 3. Note: `.env.local` is gitignored and safe for your local credentials
#
# For production configuration, see `.env.production.example`
# For setup instructions, see `README.md`
# For quick setup, see `QUICK_SETUP.md` if available

# Supabase Configuration (REQUIRED)
# Get these values from your Supabase project dashboard under Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Database Configuration (REQUIRED)
# PostgreSQL connection string for Drizzle ORM
# For local development, you can use a local Supabase instance or a cloud development instance
# Use the connection pooler for serverless environments (port 6543) or direct connection (port 5432)
# Tip: For local development, consider using Supabase CLI for a local instance
DATABASE_URL=postgresql://postgres.your-project:[password]@aws-0-region.pooler.supabase.com:6543/postgres

# Development Mode Settings (OPTIONAL)
# Set to 'true' to test landing-page-only mode locally (redirects all routes to landing page via middleware.ts)
PRELAUNCH=false
# Enable debug endpoints for development (useful for testing RBAC and API routes)
# WARNING: Must be set to 'false' in production for security
ENABLE_DEBUG_ENDPOINTS=true
# Enable test endpoints for development (useful for testing RLS policies and database connections)
# WARNING: Must be set to 'false' in production for security
ENABLE_TEST_ENDPOINTS=true

# Optional AI Services
# API key for Anthropic AI features (optional - application works without this)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# API key for Perplexity AI features (optional - application works without this)
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# Development-Specific Variables
# Usually auto-set by Next.js in development mode
NODE_ENV=development
