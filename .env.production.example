# Production Environment Variables Template
# 
# This file serves as a template for production environment variables.
# DO NOT commit actual production values to version control.
#
# Copy this file and set the actual values in Vercel Dashboard:
# Project Settings → Environment Variables
#
# For complete deployment instructions, see VERCEL_DEPLOYMENT.md

# =============================================================================
# Supabase Configuration (Required)
# =============================================================================
# Get these from your Supabase project dashboard:
# Settings → API → Project URL and API Keys
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# Database Configuration (Required)
# =============================================================================
# Use the connection pooler (port 6543) for serverless compatibility
# Format: postgresql://postgres.[project-ref]:[password]@[region].pooler.supabase.com:6543/postgres
# Get this from Supabase Dashboard → Settings → Database → Connection String → Connection Pooling
DATABASE_URL=postgresql://postgres.your-project-ref:your-password@your-region.pooler.supabase.com:6543/postgres

# =============================================================================
# Security Configuration (Required)
# =============================================================================
# NEVER enable these in production
ENABLE_DEBUG_ENDPOINTS=false
ENABLE_TEST_ENDPOINTS=false

# =============================================================================
# Application Configuration (Optional)
# =============================================================================
# Set to 'true' to enable landing-page-only mode (blocks app routes)
# When enabled, only landing pages and static assets are accessible
# App routes will redirect to the landing page
PRELAUNCH=false

# =============================================================================
# AI Services Configuration (Optional)
# =============================================================================
# These are only needed if you're using AI-powered features
ANTHROPIC_API_KEY=your_anthropic_api_key_here
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# =============================================================================
# Vercel Deployment Notes
# =============================================================================
#
# 1. All environment variables must be set in Vercel's dashboard:
#    Project Settings → Environment Variables
#
# 2. DATABASE_URL should use the connection pooler (port 6543) for serverless
#    compatibility. Direct connections (port 5432) may cause connection limit
#    issues in serverless functions.
#
# 3. The SKIP_ENV_VALIDATION variable is automatically set by vercel.json
#    and should NOT be manually configured in Vercel dashboard.
#
# 4. Mark sensitive variables as "Sensitive" in Vercel to prevent exposure
#    in logs:
#    - SUPABASE_SERVICE_ROLE_KEY
#    - DATABASE_URL
#    - ANTHROPIC_API_KEY
#    - PERPLEXITY_API_KEY
#
# 5. For complete deployment instructions, see VERCEL_DEPLOYMENT.md
#
# =============================================================================
