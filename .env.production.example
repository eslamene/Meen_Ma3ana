# Production Environment Variables Template
#
# This file serves as a template for production environment configuration.
# Copy this file to .env.production and fill in your actual values.
# NEVER commit .env.production to version control, as it contains sensitive credentials.
#
# For development configuration, see `.env.example`
# For setup instructions, see `README.md`

# Supabase Configuration (REQUIRED)
# These values are found in your Supabase project dashboard under Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Database Configuration (REQUIRED)
# PostgreSQL connection string for Drizzle ORM
# Use the connection pooler for serverless environments (port 6543)
# For direct connections, use port 5432
# Found in Supabase dashboard: Settings > Database > Connection string
DATABASE_URL=postgresql://postgres.your-project:[password]@aws-0-region.pooler.supabase.com:6543/postgres

# Application Mode (REQUIRED for Pre-launch)
# Set to 'true' to enable landing-page-only mode, redirecting all routes to landing page
# Set to 'false' to enable the full application
# This is handled by middleware.ts
# Default: false (for production after launch)
PRELAUNCH=false

# Security Flags (REQUIRED)
# CRITICAL: These MUST remain false in production environments
# Setting these to true exposes debug/test endpoints that should never be accessible in production
# These flags are enforced by the application code and should NEVER be overridden in production
# WARNING: Enabling these in production poses a severe security risk
ENABLE_DEBUG_ENDPOINTS=false
ENABLE_TEST_ENDPOINTS=false

# Optional AI Services
# API key for Anthropic AI features (optional)
# Application will work without this
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# API key for Perplexity AI features (optional)
# Application will work without this
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# Next.js Configuration (AUTO-SET by Vercel)
# Automatically set to 'production' by Vercel - do not set manually
# NODE_ENV is automatically set to 'production' by Vercel

# Automatically set to '1' when deployed on Vercel - do not set manually
# VERCEL is automatically set to '1' by Vercel

# Automatically set to the deployment URL by Vercel - do not set manually
# VERCEL_URL is automatically set to the deployment URL by Vercel

# Locale Configuration (OPTIONAL)
# Default locale for the application (defaults to 'en' if not set)
# Available locales: 'en', 'ar' (see src/i18n/request.ts)
NEXT_PUBLIC_DEFAULT_LOCALE=en
